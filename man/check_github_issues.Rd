% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clone_build_check.R
\name{check_github_issues}
\alias{check_github_issues}
\alias{get_assigned_packages}
\alias{list_assigned_issues}
\alias{clone_check_github}
\alias{get_additional_packages}
\title{Clone, build, and check Bioconductor contributions directly from
GitHub}
\usage{
check_github_issues(issues, base_repo_dir = ".", output_dir = ".")

get_assigned_packages(username, org = "Bioconductor", repo = "contributions")

list_assigned_issues(issues)

clone_check_github(
  issue_number,
  org = "Bioconductor",
  repo = "contributions",
  base_repo_dir = ".",
  output_dir = "."
)

get_additional_packages(
  issue,
  org = "Bioconductor",
  repo = "contributions",
  issue_number = NULL
)
}
\arguments{
\item{issues}{\code{gh_response} / \code{list} GitHub issues obtained by
\code{get_assigned_packages}}

\item{base_repo_dir}{\code{character(1)} The base directory where the repositories
will be cloned to. Default is the current working directory.}

\item{output_dir}{\code{character(1)} The directory where the output files will be
saved. Default is the current working directory.}

\item{username}{\code{character(1)} The GitHub username of the reviewer.}

\item{org}{\code{character(1)} The GitHub organization. Default is "Bioconductor".}

\item{repo}{\code{character(1)} The GitHub repository of the issue tracker (by
default "contributions").}
}
\description{
The functions are designed to be used with the GitHub API and
the \code{gh} package. The typical workflow is to fetch issues assigned to a
specific Bioconductor reviewer. The reviewer can then extract the
repository URLs from the issue body and clone, install dependencies, build,
and check the package.
}
\details{
\itemize{
\item \code{check_github_issues}, processes a list of GitHub
issues, cloning the repositories, installing dependencies, building the
package, and checking the package.
\item \code{get_assigned_packages} fetches issues assigned to a specific GitHub
username (reviewer).
\item \code{list_assigned_issues} extracts repository URLs from the issue bodies.
\item \code{clone_check_github} clones, installs dependencies, builds, and checks a
single GitHub repository with the issue number as input.
\item \code{get_additional_packages} extracts additional repository URLs from the
\code{AdditionalPackage} comment within an issue.
}
}
\examples{
reviews <- get_assigned_packages("LiNk-NY")
check_github_issues(reviews, base_repo_dir = "~/reviews")
list_assigned_issues(reviews)

clone_check_github("3039", base_repo_dir = "~/reviews")

\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
## pull any additional packages
get_additional_packages(reviews[[2L]])
\dontshow{\}) # examplesIf}
}
